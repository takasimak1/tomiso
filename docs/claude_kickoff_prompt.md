# Claude キックオフプロンプト（tomiso / ととレジ）

Mac再起動後など、Claude Codeとの作業を再開するときにこのままコピー＆ペーストして使う。
作業が一区切りつくたびに、下記「直前までの状況」を更新しておくこと。

---

## コピー用プロンプト

```
tomiso プロジェクトで作業を再開します。
リポジトリ: ~/Documents/Claude/Projects/富惣/FileMakerDataAPI/富惣_FileMakerDataAPI/tomiso/
（CLAUDE.md にプロジェクト概要・デプロイ手順あり。まずそれを読んでください）

直前までの状況:
（ここに docs/claude_kickoff_prompt.md の「直前までの状況」セクションを貼り付ける）

まずローカルのファイルアクセス（ls / Read）とgitの状態（git status / git log）を
確認してから、上記の続きに着手してください。
```

---

## 直前までの状況（最終更新: 2026-08-30）

-5. **hq_store.php（店舗別集計）に「上代」「上代達成率」列を追加（完了・本番デプロイ済み）**
   `daily_report_API`（メインレイアウト）使用のためフィールド追加は不要。日別
   テーブル・月合計行・CSVエクスポートに対応。部門別サブ行・未確定/定休日の
   colspan統合表示も列数増加に合わせて調整。
   **副産物のバグ修正**: 未来日／データなし行（is_future/elseブランチ）が
   既存列より1つ多いセル数を出力しており、表がずれる既存バグを発見（列を
   追加する過程で気づいた）。あわせて修正済み。
   実店舗（101・2026年8月、ユーザー指定のURLと同一条件）で読み取り専用の
   動作確認済み（エラーなし、全行9列で正しく整列）。push・本番デプロイ済み。

-4. **hq_nyuryoku.php（投入確認）に定休日表示を追加（完了・本番デプロイ済み）**
   全店日別売上マトリクスの各セルで、定休日=1の日は「未確定」と同様の優先度で
   「定休」表示に切り替え（画面・Excelエクスポート両方）。この画面は
   `daily_report_API`（メインレイアウト）を使用しているため追加フィールド配置は
   不要だった。架空店舗番号990・今月分のテストレコードで画面・Excel両方確認後、
   削除。push・本番デプロイ済み。これで店舗側（daily_report_entry.php）・本社側
   （hq_store.php・hq_nyuryoku.php）・自店月次（daily_report_mystore.php）
   すべてで定休日の区別表示に対応完了（hq_jikanbetsu.phpのみ未対応、全店集計の
   ため優先度低いと判断し据え置き）。

-3. **daily_report_mystore.php に「上代」「上代達成率」列を追加（完了・本番デプロイ済み）**
   店舗向け月次サマリー画面（合計売上/客数/前年/昨対の日別テーブル）に、
   合計売上と客数の間に「上代」「上代達成率」（＝合計売上÷上代）列を追加。
   月計行にも同様に追加。定休日の日はcolspan統合表示（6列分）に対応。
   **重要な発見**: この画面は`daily_report_API`ではなく別レイアウト
   `daily_report_sum_API`を使っており、確認したところ`上代合計`・`定休日`
   フィールドがこのレイアウトには配置されていなかった（フィールド自体は
   存在するがレイアウトに未配置）。そのため前回実装した「定休日」の
   区別表示（🏠 定休日）もこのファイルでは常に効かない状態だった
   （気づかず実装していたバグ）。ユーザーに両フィールドを
   `daily_report_sum_API`にも追加してもらい解消（同一セッション内で対応）。
   本番相当のFileMaker DataAPIで実店舗（101）の実データを読み取り専用で
   確認済み（書き込みなし、エラーなし）。push・本番デプロイ済み。

-2. **定休日入力（新規実装・完了・本番デプロイ済み。未来日対応・一覧アコーディオン追加済み）**
   `daily_report_entry.php` に「🏠 この日を定休日として確定する」ボタンを新設
   （未確定の日のみ表示）。押すと部門別売上・客数（12時/15時/17時/閉店後）を
   すべて0にして`入力状態`='確定'で即確定（`daily_report_API.定休日`=1、数値
   フィールド。ユーザーがFM側に追加済みで確認済み）。確定解除（🔓）時は定休日
   フラグも0に戻す。0円確定の通常営業日と区別するため、`hq_store.php`
   （画面・CSV両方）と`daily_report_mystore.php`で「🏠 定休日」表示に切り替え。

   追加要望（同日中）で以下を実装:
   - 未来の日付（最大30日先、日付ナビの上限と同じ）でも定休日設定を可能にした
     （通常の売上入力は引き続き未来日ブロックのまま。定休日ボタンのみ例外）。
     自店舗のみが対象。
   - 日報画面に「📅 定休日一覧」アコーディオンを追加。自店舗の登録済み定休日を
     全件（過去分はグレー表示）、クリックでその日の日報画面へ遷移できる形で表示。

   本番相当のFileMaker DataAPIで実際に確認済み（架空店舗番号990・未来日での
   テストレコード作成→表示確認→削除、という手順で実店舗データに触れずに検証）。
   push・本番デプロイ済み。
   **未確認**: hq_jikanbetsu.php（全店集計）・hq_nyuryoku.php（投入確認）は対応
   範囲外のまま（全店集計・確認ステータス表示のため定休日の区別は優先度低いと判断）。
   ブラウザでの実際の見た目（API経由の確認のみ）。

   **注意（要フォロー）**: 本機能の動作確認中、誤って実店舗（101・あべの）の
   本日分の実レコードに上代合計=123456というテスト値を書き込んでしまい、
   気づいて0（空欄相当）に戻す修正を行った。他のフィールドは触れていない。
   ユーザーへ報告済み。今後は本番データに書き込むテストを避け、未来日や
   架空店舗番号での検証に切り替えた。

-1. **単品管理ダッシュボード本体（新規実装・完了・本番デプロイ済み）**
   `hq_tanpin.php` を新規作成。クライアント提供のExcel（`26.06.21 ととレジ単品管理
   ダッシュボード.xlsx`、①/②-1/②-2/②-3/③シート）の仕様を、5シートではなく
   「1画面2タブ＋ドリルダウン」に統合して再現（ユーザー判断で画面数を圧縮）：
   - タブ「推移」＝全店合計を日別集計（①相当）
   - タブ「商品・店舗分析」→「商品別」＝商品ランキング、部門絞り込み可（②-1/②-2相当）。
     商品名クリックで店舗別内訳にドリルダウン（②-3相当）
   - タブ「商品・店舗分析」→「店舗別」＝選択店舗の商品別内訳（③相当）
   データソース: pos_API（値引額=0/>0で定価・値引を判定）＋ haiki_API（廃棄数）＋
   daily_report_API（上代）。

   **上代の仕様は2回訂正が入った**:
   1回目: 当初CLAUDE.md記載の「hanbai_API.上代単価（商品ごと固定値）× 合計数量」で
   実装 → ユーザーより「商品マスタの上代単価は今回使わない」と訂正、
   `daily_report_API.上代_◯◯`（部門別・日次目標、当時は複数フィールド）の
   積み上げに変更（commit 5ddee1e）。
   2回目: `daily_report_entry.php` の上代入力欄自体が「部門別」から「合計のみ」の
   単一入力に変更されたため（`上代合計`をFM計算フィールドから通常の数値フィールドに
   変更し、直接書き込む方式に）、ダッシュボード側も店舗×日の単一値
   （`daily_report_API.上代合計`）を積み上げる方式に再修正。
   → 上代は「全部門・全商品」の範囲と一致する場合のみ意味を持つため、
   「推移」タブは行ごとに、「商品別（全部門）」「店舗別」は合計行にのみ表示。
   部門絞り込み時と単品→店舗ドリルダウン（②-3）は店舗単位の合計しか無く
   部門・商品を特定できないため「―」表示（注意書きを表示）。

   本番相当のFileMaker DataAPI (sys.kei1.me) に接続してローカル動作確認済み
   （推移/商品別/部門絞り込み/ドリルダウン/店舗別/CSV書き出し、実データでの
   上代表示含めいずれもエラーなし）。`hq_header.php`/`hq_top.php` にナビ追加、
   `SPEC.md` 更新済み。push・本番デプロイ済み。
   **未確認**: ブラウザでの実際の見た目（Playwright/chromium-cli未導入のためAPI経由の
   動作確認のみ）。

0. **横向きタブレット2カラムレイアウト（実機確認完了）**
   下記8番の実機確認が完了。意図通り左に商品ボタン・右にカートが表示されることを確認済み。

0-1. **「上代」入力欄（完了・本番デプロイ済み。部門別→合計のみに仕様変更済み）**
   `daily_report_entry.php` に、売上とは別に店舗が毎日1回「目標額」を入力できる
   セクションを追加（時間帯に関係なく確定後も編集可）。当初は部門ごとに20項目
   入力する仕様だった（`上代_◯◯`、`docs/joudai_fields_import.csv`で一括作成）が、
   ユーザー判断で「合計のみ」の単一入力に簡素化。`上代合計`はFM計算フィールド
   （`上代_◯◯`の自動合計）だったが、単一入力に伴いユーザーがFM側で通常の数値
   フィールドに変更済み（API直接書き込みに対応）。`上代_◯◯`の20フィールドは
   FM側に残存するが未使用（データ移行なし、今後分から`上代合計`のみ運用）。
   本番相当のFileMaker DataAPIで実際に保存・再取得を確認済み。push・デプロイ済み。

0-2. **その他デプロイ済み修正（コミットのみでdocs反映漏れだったもの）**
   - `9ad77d2`（7/24）未確定日は前年の数字も非表示に統一（daily_report_mystore.php/hq_store.php）
   - `7fd6a76`（8/5）FileMaker Data API向けにSECOM TLS RSA Root CA 2024の
     信頼済みCAバンドルを追加（FMサーバー証明書更新に伴うSSL検証エラー対応）
   - `21188db`/`d90290e`（8/16, 8/20）sales_entry.php: dvh未対応の古いAndroid端末で
     スクロール不能になる不具合を修正（calc(100vh - 48px)をフォールバックとして先に宣言）

1. **未確定日の売上数字を非表示化（完了・デプロイ済み / commit 80f87c4）**
   `hq_nyuryoku.php` の規則（`入力状態`が「確定」でないレコードは実数値を見せず
   「未確定」表示）を `daily_report_mystore.php` / `hq_store.php` /
   `hq_jikanbetsu.php` にも展開。`hq_jikanbetsu.php`は全店集計のため、
   1店舗でも未確定があればその日の行全体を未確定扱いにし、部分集計を
   見せないようにした（CSV/Excel書き出しも同様にマスク）。前年参考値は
   確定状態に関係なく表示継続。実データ（7/23分を未確定のまま残していた
   店舗が実在）とテストレコードの両方で表示確認済み。

2. **廃棄数入力画面（商品単位）・上代単価フィールド（完了・デプロイ済み / commit d46fc70）**
   クライアントLINE回答により、単品管理ダッシュボードの仕様が確定：
   - 上代＝上代単価（商品マスタ固定値・本部設定）× 合計数量（定価数+値引数+廃棄数）で都度計算。上代達成率＝(定価売上+値引売上)/上代。クライアント検算例と一致確認済み
   - 合計列＝仕込み数（陳列ケース投入数）で確定。過去データ遡及は不要、今後分から記録開始
   - 廃棄数は当初「部門単位」で判断していたが、単品別ドリルダウン（②-1/②-3/③シート）が
     成立しないため**商品単位**に変更（ユーザー判断）
   FileMaker側に `hanbai_API.上代単価`（数値）と新規レイアウト `haiki_API`
   （fk_店舗No/店舗名/売上日/商品名/廃棄数、商品×日付で1レコード）を作成。
   `haiki_entry.php` を新規実装（店舗の取扱商品を部門別一覧表示、廃棄数入力、
   0件に戻すとレコード削除、未来日入力不可）。`top.php`に導線追加。
   実データ相当のcreate/edit/delete一連の動作をテスト店舗(999)で確認済み。
   → 次はダッシュボード本体（pos_API＋haiki_API＋hanbai_APIを突合し、
   エクセルの①〜③の切り口を再現する画面）の設計・実装。

3. **売上日報にととレジ実績列を追加（完了・デプロイ済み / commit 3b88e1a）**
   `daily_report_entry.php` の12時/15時/17時/閉店後の各セクションに、`pos_API`明細を
   `作成情報タイムスタンプ`で時刻カットオフ集計した「ととレジ実績」列（緑色・参照専用）
   を追加。客数はレシート番号のdistinct件数、累計売上は販売金額合計。既存の
   本年入力／前年同曜日と並べて4列グリッドで比較できるようにした。実データ
   （店舗124・07/20付）で表示確認済み。

4. **レシート印刷: 部門小計・バーコード幅拡大（完了・デプロイ済み / commit b34b2b0, dc33e0e）**
   `star_webprnt_inline.php` に部門名行右端の部門小計表示を追加し、バーコード幅を
   width2→width3に拡大。実機試し印刷で小計行の折り返し不具合を発見し、1行の
   文字数上限を48→46に補正して解消。

5. **部門マスタ統一（完了・デプロイ済み / commit 620c716）**
   FileMakerレイアウト `bumon_API`（部門CD/部門名/並び順）を新設し、`bumon_master.php`
   を新規作成。`hq_shohin_maint.php` / `hq_tenpo_maint.php` / `sales_entry.php` /
   `sales_edit.php` / `shohin_maint.php` の部門定義をこのマスタ参照に統一。

6. **インストアコード取得バグ修正（完了・デプロイ済み / commit 620c716）**
   `sales_entry.php` の `_fetch_instore_codes_from_fm()` と `sales_edit.php` の
   `_fetch_instore_codes_from_fm_edit()` が、存在しないメソッド
   `$fm->findRecord(...)` を呼んでいたため、account_API からのインストアコード取得が
   常にサイレントに失敗していた（try/catchで握りつぶされていた）。
   `$fm->findRecords(['query' => [...]])` に修正して解消。
   → 難波店のレシートでバーコードが出ない不具合の原因だった。修正後、印刷・プレビュー
   とも表示確認済み。

7. **UI微修正（完了・デプロイ済み / commit c77bbc1）**
   - `top.php`: ナビボタン「売上登録」→「レジ」に表示変更
   - `sales_entry.php`: レシート画面の「次のお客様へ」ボタンを廃止し、印刷ボタン1つで
     印刷実行と同時に売上登録画面へ戻るよう統合（未使用になった `.rcpt-btn-next` の
     CSSも削除）
   - `sales_entry.php`: 「全消去」「登録する」ボタンに `user-select: none` /
     `-webkit-touch-callout: none` を追加し、タブレット長押し時のコピー/共有メニュー
     表示を抑止

8. **横向きタブレット2カラムレイアウト（完了・デプロイ済み / commit 2d1cf93）**
   `sales_entry.php` のレジ画面に `@media (orientation: landscape)` を追加し、
   左63%（タブ＋商品グリッド）・右37%（カート全高）のCSS Gridレイアウトに変更。
   縦向きは既存レイアウトを完全維持。対象タブレットは実測 23.7cm×14.8cm
   （アスペクト比約1.6、店舗の専用端末のみで使用するため、幅指定ではなく
   `orientation: landscape` のみで判定）。
   **未確認**: 実機タブレットを横向きにして、左に商品ボタン・右にカートが
   意図通り表示されるかの実機確認待ち。

## 次にやること候補
- **単品管理ダッシュボード（-1番）をブラウザで見た目確認 → push → 本番デプロイ**
- 0-1番（上代入力欄）の本番デプロイ（push済み前提でbash deploy.sh実施）
- ととレジ実績列（3番）、他の店舗・繁忙日でも表示崩れがないか、余裕があれば確認

## 参照
- プロジェクト概要・デプロイ手順: `CLAUDE.md`
- 詳細仕様: `SPEC.md`
- デプロイ: `bash deploy.sh`（rsyncでさくらインターネットへ反映。`docs/` はデプロイ対象外）
